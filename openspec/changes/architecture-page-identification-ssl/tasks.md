# Tasks

## Task 1: Add content-property tagging to page creation

**File**: `publisher/pagesController.py`

- [x] Add `_set_page_property(page_id, login, password)` that POSTs to `/content/{page_id}/property` with key `autogenerated-publisher` and a JSON value payload.
- [x] Call `_set_page_property()` from `createPage()` after the page is created and the id is returned.
- [x] Remove the search-pattern title suffix from `createPage()` (keep the title clean).
- [x] Add config field `publisher_property_key` with default `autogenerated-publisher` to `publisher/config/config.yaml` and `getconfig.py`.

## Task 2: Switch search to property-based CQL

**File**: `publisher/pagesController.py`

- [x] Rewrite `searchPages()` to use CQL: `type = page AND space = "<SPACE>" AND ancestor = <PARENT_ID> AND content.property[<KEY>].publisher = "github-confluence-publisher"`.
- [x] Add a legacy fallback: if property-based search returns zero results, run the old text-based CQL once to catch pages from prior runs. Log a warning when fallback finds pages.
- [x] Remove `confluence_search_pattern` from `publisher/config/config.yaml` after fallback is no longer needed (keep it for the migration period).

## Task 3: Remove `verify=False` and SSL warning suppression

**File**: `publisher/pagesController.py`

- [x] Remove `verify=False` from all five `requests` calls (GET parent, POST create, GET search, DELETE, POST attach).
- [x] Remove the `requests.packages.urllib3.disable_warnings(category=InsecureRequestWarning)` line.
- [x] Remove the `from urllib3.exceptions import InsecureRequestWarning` import if no longer used.

## Task 4: Create Dockerfile

**File**: `Dockerfile` (new, project root)

- [x] Base image `python:3.14-slim`.
- [x] Install `ca-certificates` and run `update-ca-certificates`.
- [x] Copy `publisher/` and `requirements.txt`, install deps.
- [x] Support custom CA certs via mount point `/usr/local/share/ca-certificates/custom/`.
- [x] Add entrypoint script that runs `update-ca-certificates` (to pick up any mounted certs) then exec's `python publisher/main.py`.
- [x] Document env vars in a comment block or label.

## Task 5: Make banner configurable

**Files**: `publisher/pagesController.py`, `publisher/config/config.yaml`, `publisher/config/getconfig.py`

- [x] Add `banner_repo_url` and `banner_project_name` to config YAML with sensible defaults.
- [x] Add env-var overrides `BANNER_REPO_URL` and `BANNER_PROJECT_NAME` in `getconfig.py`.
- [x] Replace hardcoded banner HTML in `createPage()` with a template populated from config.

## Task 6: Update GitHub Actions workflow

**File**: `.github/workflows/publisher.yml`

- [x] Add a step to build the Docker image.
- [x] Replace the Python-install-and-run step with `docker run`, passing secrets as env vars and mounting `data/` and `data_images/`.
- [x] Keep the existing gate that skips Confluence steps on fork PRs without secrets.

## Task 7: Update architecture and spec documents

**Files**: `openspec/architecture.md`, `openspec/specs/publisher/spec.md`

- [x] Update architecture to document: content-property identification, Docker packaging, SSL verification, configurable banner.
- [x] Update spec to add/revise requirements and scenarios for: property-based identification, SSL verification enabled, configurable banner, Docker container inputs.
- [x] Remove references to `confluence_search_pattern` and `verify=False` as accepted tradeoffs.

## Task 8: Add/update tests

**Files**: `publisher/test_*.py`

- [x] Add unit tests for `_set_page_property()`.
- [x] Update existing tests that reference the search-pattern title suffix.
- [x] Add test for banner template rendering with custom config values.
